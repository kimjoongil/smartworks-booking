export const GET = async (req: Request, res: Response) => {
  console.log("GET /api/displays");
};

export const POST = async (req: Request, res: Response) => {
  console.log("POST /api/displays");
};
